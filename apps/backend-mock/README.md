# @vben/backend-mock

## Description

Vben Admin 的 mock 服务。当前仓库里，`web-admin` 的系统管理主流程已经切到正式后端接口，开发环境默认通过 `VITE_NITRO_MOCK=false` 关闭 Nitro Mock。这里仅保留未正式化模块仍需要的最小 mock 集合。

## Running the app

```bash
# development
$ pnpm run start

# production mode
$ pnpm run build
```
